# SIC Assembler

## install dependecies
```sh
$ git submodule update --init
$ cmake --preset install
$ mkdir build
$ cmake --preset default
```

## build
```sh
$ cmake --build build
```

## usage

```sh
$ ./bin/sic-assembler <asm file>
```